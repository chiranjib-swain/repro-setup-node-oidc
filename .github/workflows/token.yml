on:
  workflow_dispatch:

jobs:
  token-demo:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      

      - name: Attempt to print token (will be masked)
        shell: bash
        run: |
          echo "GITHUB_TOKEN=${GITHUB_TOKEN}"


